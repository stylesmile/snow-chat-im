<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.stylesmile.chat.mapper.ChatSessionMapper">
    <resultMap id="chatSession" type="com.stylesmile.chat.entity.ChatSession"/>

    <!-- 查询用户的会话列表 -->
    <select id="getSessionsByUserId" resultMap="chatSession">
        SELECT *
        FROM chat_session
        WHERE user_id = #{userId}
        ORDER BY last_msg_time DESC, update_time DESC
    </select>

    <!-- 更新最后一条消息 -->
    <update id="updateLastMessage">
        UPDATE chat_session
        SET last_msg = #{lastMsg},
            last_msg_time = NOW(),
            unread_count = unread_count + 1,
            update_time = NOW()
        WHERE user_id = #{userId}
          AND target_id = #{targetId}
          AND target_type = #{targetType}
    </update>

    <!-- 清除未读消息数 -->
    <update id="clearUnreadCount">
        UPDATE chat_session SET unread_count = 0 WHERE user_id = #{userId} AND target_id = #{targetId}
    </update>
</mapper>
